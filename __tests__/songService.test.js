jest.mock("../src/repository/songDAO")

describe("Test suite for getSongs", () => {
    test("No tests for getSongs function at the moment", () => {
        // A lot of postman tests because of the wide range of queries possible
        expect(1).toBe(1);
    })
})